# ============================================================================
# Akash AI Pro - Secure Enterprise API Gateway
# Production dependency manifest. Pinned for reproducible builds.
# ============================================================================

# --- Web Framework -----------------------------------------------------------
fastapi==0.111.0
uvicorn[standard]==0.30.1
starlette==0.37.2

# --- Data Validation ----------------------------------------------------------
pydantic==2.7.4
pydantic-settings==2.3.4

# --- Security / Auth ------------------------------------------------------
pyjwt[crypto]==2.8.0
passlib[bcrypt]==1.7.4
# NOTE: bcrypt is pinned to 4.0.1 (not the latest 4.1.x) because passlib
# 1.7.4's bcrypt backend probes `bcrypt.__about__.__version__`, which was
# removed in bcrypt>=4.1.0. This is a known upstream passlib/bcrypt
# compatibility issue -- pinning here avoids a runtime crash on hashing.
bcrypt==4.0.1
cryptography==42.0.8
python-multipart==0.0.9

# --- Redis (async) ----------------------------------------------------------
redis==5.0.6

# --- HTTP client (used by dashboard + circuit breaker health pings) --------
httpx==0.27.0

# --- Dashboard (Streamlit Enterprise UI) ------------------------------------
streamlit==1.36.0
plotly==5.22.0
pandas==2.2.2
requests==2.32.3
streamlit-autorefresh==1.0.1

# --- Observability / Logging -------------------------------------------------
python-json-logger==2.0.7

# --- Testing / CI -----------------------------------------------------------
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
fakeredis==2.23.2
flake8==7.0.0
black==24.4.2
